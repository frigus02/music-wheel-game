{
  "name": "music-wheel-game",
  "version": "0.0.1",
  "private": true,
  "description": "HTML5 music game.",
  "license": "MIT",
  "dependencies": {
    "redux": "^4.0.4"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "cpy": "^7.3.0",
    "del": "^5.1.0",
    "prettier": "^1.18.2"
  },
  "scripts": {
    "start": "browser-sync start --server --ss src --ss . --files src",
    "start:dist": "browser-sync start --server --ss dist --files dist",
		"build": "node ./build.js",
		"format": "prettier --write \"**/*.{html,js,css,md}\""
  }
}
