apiVersion: v1
kind: Service
metadata:
  name: music-wheel-game
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: music-wheel-game
